<?php

require_once "database.php";

$lines = File("Decision.txt");

// Look through the tools

// If the tool is available then say it makes all the decisions

// IF the tool isn't available then just track the ones that it actually makes

// MAKES might be the wrong word to describe this...

?>